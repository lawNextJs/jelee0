// export default function Loading({params: {id}}: {params: {id: string}}) {
//     return <h1>{`Loading... ${id}`}</h1>
// }

export default function Loading() {
    return (
        <h1>Loading Movie Detail...</h1>
    )
}
